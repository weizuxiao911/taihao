# SKILL: advance
# 用途:驱动底盘前进距离(仿真下 mock 状态变化)

name = "advance"
version = "0.1.0"
description = "前进 distance 米(mock 状态变化)"

sha256 = "advance-skill-stub-sha256-0001"
entrypoint = "advance"

input_schema = """
{
  "type": "object",
  "properties": {
    "distance": { "type": "number", "default": 1.0, "minimum": 0 }
  }
}
"""