# SKILL: echo
# 格式:Anthropic Agent Skills 兼容(简化版)
# 用途:测试用,agent 输入文本,SKILL 返回确认

name = "echo"
version = "0.1.0"
description = "内置 echo SKILL:返回输入文本的确认,用于冒烟验证"

# 签名摘要(白名单校验占位)
sha256 = "echo-skill-stub-sha256-0001"

# 执行入口(仿真下无真实入口,agent StubProvider 直接返回)
entrypoint = "echo"

# 输入参数 schema(JSON Schema 简化)
input_schema = """
{
  "type": "object",
  "properties": {
    "text": { "type": "string", "description": "要回显的文本" }
  },
  "required": ["text"]
}
"""